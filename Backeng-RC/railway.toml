[build]
builder = "RAILPACK"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/reservacanchas-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
