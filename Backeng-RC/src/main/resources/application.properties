
# MySQL database configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/reserva_canchas?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
#spring.datasource.username=root
#spring.datasource.password=manuelxxx

# Server configuration
server.port=8080
server.servlet.context-path=/api

# Conexión a Railway (URL pública)
spring.datasource.url=jdbc:mysql://zephyr.proxy.rlwy.net:11914/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mMURLCgGrZOvqnLydPsCoaRyFUuqHGyW
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# OpenAPI/Swagger Documentation
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/docs
springdoc.swagger-ui.operationsSorter=alpha

# Security configuration (OAuth2 JWT)
# By default, we use a mock JwtDecoder for dev/local testing to prevent startup crashes when Keycloak/Firebase is offline.
# To connect to Keycloak, uncomment the line below:
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8081/realms/canchacraft
# To connect to Firebase, uncomment the line below (replace with your Firebase project ID):
# spring.security.oauth2.resourceserver.jwt.issuer-uri=https://securetoken.google.com/YOUR_FIREBASE_PROJECT_ID